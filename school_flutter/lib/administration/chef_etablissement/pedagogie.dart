import 'package:flutter/material.dart';



/// ----------------------
/// 5. Pédagogie
/// ----------------------
class PedagogiePage extends StatelessWidget {
  const PedagogiePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Gestion Pédagogique",
        style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
      ),
    );
  }
}